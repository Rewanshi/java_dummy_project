{
  "name": "dummy-ts",
  "version": "1.0.0",
  "scripts": {
    "test": "jest --ci --reporters=default --reporters=jest-junit",
    "build": "tsc"
  },
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "typescript": "^5.0.0",
    "jest-junit": "^15.0.0",
    "junit-to-sarif": "^0.4.0"
  },
  "jest-junit": {
    "outputDirectory": ".",
    "outputName": "junit.xml"
  }
}

  